[package]
name = "pre-usage"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "pre-usage"
path = "src/main.rs"

[dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
colored = "2"
toml = "0.8"
